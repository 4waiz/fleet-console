# Optional app branding
NEXT_PUBLIC_APP_NAME=Fleet Console

# Optional Vercel KV / Upstash Redis bindings
# If omitted, the app automatically falls back to in-memory storage.
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
